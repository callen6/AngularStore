Items in your cart:
<% @line_items.each do |item| %>
<div><%= item.product_id%></div>
<div><%= button_to "Remove From Cart", remove_item_from_cart_path(line_item_id: item), method: :delete, class: "button tiny" %> </div>
<% end %>

<%= button_to "Checkout", new_order_path, class: "button" %>